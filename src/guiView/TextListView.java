package guiView;

import javax.swing.*;
import java.awt.*;

public class TextListView extends JPanel {
    public TextListView() {
        setLayout(new BorderLayout());
        setBackground(Color.WHITE);
    }
}
